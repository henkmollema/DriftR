namespace Driftr
{
    internal static class GameSettings
    {
        public const float MaxSpeed = 250.0f;

        public const float EmptyFuelSpeed = 30.0f;

        public const float Throttle = 3.5f;

        public const float DisplaySpeedModifier = 2.0f;
    }
}
